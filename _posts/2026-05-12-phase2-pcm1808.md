---
layout: post
category: fpga
title: "Фаза 2: Захоплюємо живий звук (PCM1808)"
date: 2026-05-12
toc: true
permalink: /posts/phase2-pcm1808/
---

Є вихід. Тепер потрібен **вхід** — ADC PCM1808 від Texas Instruments.

---

## Нюанси PCM1808

Модуль HW-935 з PCM1808 на борту. Всі потрібні піни виведені, є вбудований LDO регулятор на 3.3V:

<a href="/assets/images/pcm1808.png" class="glightbox" style="display: block;" data-description="PCM1808 ADC модуль (HW-935)">
  <img src="/assets/images/pcm1808.png" alt="PCM1808 ADC модуль (HW-935)" style="cursor: zoom-in; max-width: 380px;">
</a>

**Подвійне живлення:** аналогова частина — 5V, цифрова (I²S логіка) — 3.3V. Пропустити цей факт — чіп або не запустить, або дасть шум.

Модуль HW-935 має вбудований **LDO регулятор** — мікросхема яка знижує напругу (5V → 3.3V). Тобто подаєш 5V на VCC, а 3.3V для цифрової частини модуль бере сам.

**Увага — I²C і рівні напруги:** якщо живити ADS1115 від 5V — pull-up резистори на SDA/SCL підтягуються до 5V. FPGA очікує 3.3V. Cyclone IV має 5V-tolerant піни, але краще живити ADS1115 від 3.3V щоб I²C логіка теж була 3.3V.

**Назви:** TI в документації називає тактовий вхід **BCK** (Bit Clock). В I²S це те саме що **SCLK**. Просто знати треба.

---

## I²S приймач у Verilog

PCM1808 надсилає аудіо побітово по лінії DOUT — один біт на кожен такт SCLK. Завдання FPGA: зібрати ці біти в число.

**Крок 1 — збираємо біти в зсувний регістр.**

На кожному спадному фронті SCLK біт з DOUT "заштовхується" з правого краю, а решта зсувається вліво:

```
До:    rx_buf = [b23 b22 b21 ... b1  b0 ]
Після: rx_buf = [b22 b21 b20 ... b0  dout]
```

```verilog
always @(posedge clk)
    if (sclk_rise)
        rx_buf <= {rx_buf[22:0], dout};  // зсуваємо вліво, новий біт справа
```

**Крок 2 — знімаємо готовий семпл.**

PCM1808 передає 24 біти на семпл. Коли всі 24 біти зібрані — захоплюємо верхні 16 біт:

```verilog
always @(posedge clk)
    if (sclk_fall)
        if (bit_cnt == 6'd24) left_cap <= rx_buf[23:8];  // беремо старші 16 біт
```

**Чому тільки 16 біт з 24?**

Свідомий вибір, не обмеження. 24-бітна обробка потребувала б більше M9K пам'яті (delay lines стали б в 1.5 рази більшими) і більше логіки для множення. При цьому 16 біт — це CD якість (96 dB динамічний діапазон), чого більш ніж достатньо:

```
16 біт → динамічний діапазон 96 dB   ← достатньо для навушників
24 біт → динамічний діапазон 144 dB  ← студійна якість
```

Молодші 8 біт — шум АЦП на рівні ~-120dB. Для spring reverb ефекту не критично.

Чому `bit_cnt == 24`, а не `23`? Через той самий **1-bit gap** стандарту Philips I²S — MSB приходить на такт пізніше після зміни LRCLK, тому повний семпл готовий на такт 24, а не 23.

---

## Результат

Телефон → PCM1808 → FPGA → CS4344 → навушники.

Цифровий pass-through живого аудіо. Затримка — менше 1 мс.

---

[← Фаза 1: CS4344](/posts/phase1-cs4344/) \| [Фаза 3: Гучність →](/posts/phase3-volume/)
